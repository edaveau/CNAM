CREATE SCHEMA raw_data
AUTHORIZATION postgres;


CREATE TABLE raw_data.raw_data_aggregated(
  REG          INTEGER  NOT NULL PRIMARY KEY, 
  LIBREG       VARCHAR(30),
  DEP          INTEGER  NOT NULL,
  LIBDEP       VARCHAR(30),
  EPCI         INTEGER  NOT NULL,
  LIBEPCI      VARCHAR(25) NOT NULL,
  DROIT        BIT  NOT NULL,
  DEPCOM       INTEGER  NOT NULL,
  CODEPOSTAL   INTEGER  NOT NULL,
  LIBCOM       VARCHAR(9) NOT NULL,
  NUMVOIE      VARCHAR(30),
  INDREP       VARCHAR(30),
  TYPVOIE      VARCHAR(5),
  NOMVOIE      VARCHAR(12),
  NUMAPPT      VARCHAR(4) NOT NULL,
  NUMBOITE     VARCHAR(30),
  ESC          VARCHAR(30),
  COULOIR      VARCHAR(30),
  ETAGE        INTEGER ,
  COMPLIDENT   VARCHAR(30),
  ENTREE       VARCHAR(1),
  BAT          VARCHAR(10),
  IMMEU        VARCHAR(20) NOT NULL,
  COMPLGEO     VARCHAR(30),
  LIEUDIT      VARCHAR(30),
  QPV          INTEGER  NOT NULL,
  TYPECONST    VARCHAR(1) NOT NULL,
  NBPIECE      INTEGER  NOT NULL,
  SURFHAB      INTEGER  NOT NULL,
  CONSTRUCT    INTEGER  NOT NULL,
  LOCAT        INTEGER  NOT NULL,
  PATRIMOINE   INTEGER  NOT NULL,
  ORIGINE      INTEGER  NOT NULL,
  FINAN        INTEGER  NOT NULL,
  FINANAUTRE   VARCHAR(14),
  CONV         INTEGER  NOT NULL,
  NUMCONV      VARCHAR(30),
  DATCONV      VARCHAR(30),
  NEWLOGT      VARCHAR(30),
  CUS          VARCHAR(30),
  DPEDATE      VARCHAR(7) NOT NULL,
  DPEENERGIE   VARCHAR(30),
  DPESERRE     VARCHAR(30),
  SRU_EXPIR    VARCHAR(30),
  SRU_ALINEA   VARCHAR(30),
  CODSEGPATRIM VARCHAR(30),
  LIBSEGPATRIM VARCHAR(30)
)
WITH (
	OIDS = FALSE
);